import React from "react";
import ReactDOM from "react-dom/client";
import "./index.css"; // You can create this file empty if it doesn't exist
import App from "./App";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(<App />);
